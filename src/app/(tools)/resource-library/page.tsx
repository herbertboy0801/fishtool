"use client";

import { useState } from "react";
import { PageHeader } from "@/components/layout/page-header";
import { store } from "@/lib/store";

interface Article {
  id: string;
  category: string;
  title: string;
  summary: string;
  readTime: string;
  tags: string[];
  content: string;
}

const ARTICLES: Article[] = [
  {
    id: "1",
    category: "新手入门",
    title: "闲鱼卖家必知的 10 条黄金法则",
    summary: "从选品到成交的完整路径，新手开店必读。",
    readTime: "5 分钟",
    tags: ["新手", "选品", "开店"],
    content: `## 10 条黄金法则

**1. 标题包含 3 个以上核心关键词**
买家通过搜索找到你，关键词决定曝光量。

**2. 主图背景简洁，商品占画面 70% 以上**
杂乱背景会降低点击率，纯色背景最佳。

**3. 定价比新品低 3-5 折，成色越好越接近新品价**
闲鱼买家追求性价比，定价过高无人问津。

**4. 详情图展示四个角度：正面、背面、细节、瑕疵**
真实展示瑕疵，减少退款纠纷。

**5. 每天 9:00-11:00 和 19:00-22:00 上架**
这两个时段流量最高，上架后优先推荐。

**6. 首次上架不设运费，后续根据订单量调整**
免邮降低买家决策门槛，提升成交率。

**7. 24 小时内回复所有咨询**
响应速度影响平台推荐权重。

**8. 积累 10+ 条好评后，可以适当提价**
信用分高的卖家，买家愿意支付溢价。

**9. 同一商品最多发 3 条动态，间隔 2 天**
过于频繁发布会被系统降权。

**10. 与买家沟通时，先认可再引导**
"您说得对，这件商品确实..." 开头效果最好。`,
  },
  {
    id: "2",
    category: "新手入门",
    title: "账号权重是什么？如何快速提升？",
    summary: "权重决定曝光，了解平台算法才能弯道超车。",
    readTime: "4 分钟",
    tags: ["权重", "曝光", "算法"],
    content: `## 账号权重解析

账号权重是闲鱼评估卖家质量的综合分，直接影响你的商品排名和推荐频率。

### 影响权重的核心因素

**正向因素（提升权重）**
- ✅ 成交率：每成功卖出一件，权重 +5
- ✅ 好评率：好评率 95% 以上享受推荐加权
- ✅ 回复速度：1小时内回复，权重持续累积
- ✅ 商品质量：主图清晰、描述详细
- ✅ 账号活跃度：每天登录、发布、互动

**负向因素（降低权重）**
- ❌ 取消交易：每次取消 -10 权重
- ❌ 被投诉：一次有效投诉 -20 权重
- ❌ 长期不活跃：7天未登录开始衰减
- ❌ 发布违禁词：系统自动降权

### 快速提升方法

1. **每天在固定时段发布**（早 10 点、晚 8 点）
2. **先从低价商品积累成交**，哪怕 1 元的
3. **认真回复每一条咨询**，不能敷衍
4. **完善个人资料**：头像、简介、实名认证`,
  },
  {
    id: "3",
    category: "运营技巧",
    title: "让商品曝光翻 3 倍的标题公式",
    summary: "拆解 100 条爆款标题，总结出可复用的写法模板。",
    readTime: "6 分钟",
    tags: ["标题", "关键词", "曝光"],
    content: `## 标题公式拆解

### 黄金公式：品牌 + 型号 + 核心卖点 + 状态词

**示例：**
- ❌ 低效写法：苹果手机出售
- ✅ 高效写法：iPhone 14 Pro 256G 暗紫 九五新 原装未换屏

### 5 类状态词，按场景选择

| 类型 | 词语 | 适用场景 |
|------|------|----------|
| 成色词 | 九九新、九成新、全新未拆 | 电子产品 |
| 原装词 | 原包装、原配件齐全 | 数码配件 |
| 稀缺词 | 限量款、绝版、停产 | 收藏品 |
| 价值词 | 低价转、半价出、亏本清 | 通用 |
| 信任词 | 正品保证、可验货、面交 | 高客单价 |

### 必须避开的词

❌ 最便宜、最好、第一（违禁词）
❌ 微信、V信、加我（被屏蔽）
❌ 超过 30 个字的标题（后缀被截断）

### 实战技巧

搜索你的商品，看前 10 条结果的标题都在用什么词，把它们的高频词组合进你的标题。`,
  },
  {
    id: "4",
    category: "运营技巧",
    title: "砍价处理话术大全（含示例）",
    summary: "应对 6 种常见砍价场景，拒绝降价同时留住买家。",
    readTime: "7 分钟",
    tags: ["话术", "砍价", "成交"],
    content: `## 砍价应对话术

### 场景一：买家说「能便宜点吗」

**低效回复：** 不能少了 / 价格已经很低了

**高效回复：**
> "这件东西我已经按最低价挂了，您看成色这么好，当初买了 [原价]，现在 [当前价] 真的很实惠了。如果您真的喜欢可以下单，我给您好好打包发货～"

---

### 场景二：买家给出极低报价

**低效回复：** 不行，差太多了

**高效回复：**
> "哈哈您这个价格我真的亏大了，这样吧，我给您少 [少的金额] 块，[最终价] 拿走，我这边包邮，怎么样？"

---

### 场景三：买家说「别人同款更便宜」

**高效回复：**
> "我理解您在货比三家，能问一下那边是什么成色吗？我这件 [说明你的优势：成色/配件/正品保障]，价格差一点点但买到的品质不一样。"

---

### 场景四：快成交时买家再砍

**高效回复：**
> "哎呀您下单吧，我刚才给您已经让了步，再少我真的没法交代了，而且这个价格我今晚就下架了，您要的话抓紧哈～"

---

### 核心原则
1. 永远不要第一次就答应砍价
2. 让步要小，幅度 5-10 元为宜
3. 每次让步都要换来一个好处（好评承诺/立即下单）`,
  },
  {
    id: "5",
    category: "爆款案例",
    title: "月销 200+ 的服装卖家是怎么做到的",
    summary: "真实案例拆解，服装类目从 0 到爆的完整路径。",
    readTime: "8 分钟",
    tags: ["案例", "服装", "爆款"],
    content: `## 服装类目爆款案例

### 背景

卖家小李，全职宝妈，2023年3月开始在闲鱼卖二手衣，到同年6月月流水突破 2 万元。

### 核心策略

**1. 精准选品：聚焦高客单价品牌**
- 只卖轻奢品牌（ZARA、H&M 高端线以上）
- 单价 150 元以上，避开低价竞争
- 专注自己穿过的款式，熟悉品牌价值

**2. 主图革命**
- 每件衣服用模特（自拍）而不是平铺
- 自然光拍摄，不过度美颜
- 细节图必拍：商标、面料、瑕疵

**3. 文案写法**
标题：[品牌] [款式] [颜色/版型] [成色] [适合身材]
例：ZARA 显腰小西装 黑色 九新 适合 S-M 码微胖小个子

**4. 主动营销**
- 每周末发一条穿搭动态，带上商品链接
- 专注回复细节问题（面料、尺码建议）

### 关键数据
- 平均回复时间：45 分钟
- 商品成交转化率：18%（行业平均 8%）
- 好评率：99.2%`,
  },
  {
    id: "6",
    category: "爆款案例",
    title: "数码 3C 卖家如何用「信任」打败低价竞争",
    summary: "高客单价商品的成交逻辑与正品建信策略。",
    readTime: "6 分钟",
    tags: ["案例", "数码", "信任"],
    content: `## 数码卖家信任建设案例

### 挑战

二手数码市场竞争激烈，价格战严重，买家对翻新机、假货高度警惕。

### 解决方案：打造信任体系

**第一层：视觉信任**
- 拍摄完整开机画面（显示序列号）
- 拍摄 About 页面（显示真实配置）
- 绝不 PS 图片，真实成色

**第二层：信息透明**
- 描述中写明购买渠道和时间
- 主动说明所有瑕疵（哪怕是极小划痕）
- 注明是否换过零件（电池/屏幕）

**第三层：服务承诺**
- 支持 [城市] 本地面交验货
- 7天有问题可以协商
- 所有配件在图片中一一展示

### 效果

商品定价比同款竞争对手高 10-15%，但成交量反而更高。买家愿意为确定性付溢价。

### 核心洞察

在二手数码领域，信任 = 溢价空间。
与其打价格战，不如把精力放在建立可信度上。`,
  },
  {
    id: "7",
    category: "平台规则",
    title: "2024 年闲鱼最新违禁词清单",
    summary: "常见触发限流的词语，发布前必查。",
    readTime: "3 分钟",
    tags: ["违禁词", "规则", "限流"],
    content: `## 违禁词分类清单

### 一级违禁（立即限流）
- 最/最新/最便宜/最好（最高级用词）
- 假/仿/A货/高仿/1:1（假货相关）
- 微信/V信/扣扣（引流到站外）
- 刷单/刷量/假评（作弊行为）

### 二级违禁（降权风险）
- 限量/秒杀/爆款（夸大描述）
- 全网最低/比官网便宜（价格对比）
- 内部价/员工价/出厂价（虚假渠道）
- 保真/100% 正品（过度承诺）

### 敏感类目词
- 医药相关：药品、疗效、治疗
- 食品相关：无添加、纯天然（需认证）
- 金融相关：理财、投资回报

### 安全替代用法

| 违禁词 | 安全替代 |
|--------|----------|
| 最便宜 | 性价比高 |
| 假货 | 不出售（不要写） |
| 微信联系 | 站内聊天 |
| 保真 | 附购买凭证 |

### 检测建议

发布前使用本工具的「违禁词检测」功能一键检测，避免限流。`,
  },
  {
    id: "8",
    category: "平台规则",
    title: "账号被封了怎么办？申诉成功率提升指南",
    summary: "常见封号原因与申诉模板，帮你找回账号。",
    readTime: "5 分钟",
    tags: ["封号", "申诉", "规则"],
    content: `## 账号申诉完整指南

### 常见封号原因

| 原因 | 比例 | 可申诉性 |
|------|------|----------|
| 发布违禁商品 | 35% | 低 |
| 频繁使用违禁词 | 25% | 高 |
| 被多人举报 | 20% | 中 |
| 引导私下交易 | 15% | 中 |
| 其他 | 5% | 高 |

### 申诉步骤

1. **判断封号类型**
   - 临时封禁（7-30天）：等待期满自动解除
   - 永久封禁：需要提交申诉材料

2. **准备申诉材料**
   - 账号注册手机号
   - 身份证正反面照片
   - 说明封号原因（简洁、诚恳）
   - 承诺书（表明不再违规）

3. **申诉模板**

> 您好，我的账号 [手机号后四位] 于 [日期] 被封禁。经排查，可能是因为 [简要原因]。我承认这一行为违反了平台规则，并诚心承诺今后严格遵守规定，不再出现类似情况。请贵平台给予一次改正机会，恢复账号正常使用。谢谢！

### 申诉技巧
- 态度诚恳，不要质疑平台决定
- 一次申诉最多提交 3 次，超过不受理
- 最佳申诉时间：工作日上午 9-11 点`,
  },
];

const CATEGORIES = ["全部", "新手入门", "运营技巧", "爆款案例", "平台规则"];

export default function ResourceLibraryPage() {
  const [activeCategory, setActiveCategory] = useState("全部");
  const [bookmarks, setBookmarks] = useState<string[]>(() =>
    store.get<string[]>("rl_bookmarks", [])
  );
  const [selectedArticle, setSelectedArticle] = useState<Article | null>(null);

  const filtered = activeCategory === "全部"
    ? ARTICLES
    : ARTICLES.filter((a) => a.category === activeCategory);

  const toggleBookmark = (id: string) => {
    const next = bookmarks.includes(id)
      ? bookmarks.filter((b) => b !== id)
      : [...bookmarks, id];
    store.set("rl_bookmarks", next);
    setBookmarks(next);
  };

  const CATEGORY_COLORS: Record<string, string> = {
    "新手入门": "text-emerald-400 bg-emerald-500/15",
    "运营技巧": "text-blue-400 bg-blue-500/15",
    "爆款案例": "text-orange-400 bg-orange-500/15",
    "平台规则": "text-red-400 bg-red-500/15",
  };

  if (selectedArticle) {
    return (
      <div>
        <PageHeader
          title={selectedArticle.category}
          subtitle={`阅读时间 ${selectedArticle.readTime}`}
        />
        <div className="px-4 py-4 pb-8">
          <div className="mb-4">
            <h1 className="text-lg font-bold leading-snug">{selectedArticle.title}</h1>
            <div className="mt-2 flex flex-wrap gap-1.5">
              {selectedArticle.tags.map((t) => (
                <span key={t} className="rounded-full bg-card px-2.5 py-0.5 text-xs text-muted">
                  #{t}
                </span>
              ))}
            </div>
          </div>
          <div className="prose prose-sm prose-invert max-w-none">
            <div className="text-sm leading-relaxed text-foreground whitespace-pre-wrap space-y-2">
              {selectedArticle.content.split('\n').map((line, i) => {
                if (line.startsWith('## ')) {
                  return <h2 key={i} className="text-base font-bold mt-4 mb-2">{line.slice(3)}</h2>;
                }
                if (line.startsWith('### ')) {
                  return <h3 key={i} className="text-sm font-semibold mt-3 mb-1 text-primary">{line.slice(4)}</h3>;
                }
                if (line.startsWith('**') && line.endsWith('**')) {
                  return <p key={i} className="font-semibold">{line.slice(2, -2)}</p>;
                }
                if (line.startsWith('> ')) {
                  return (
                    <blockquote key={i} className="border-l-2 border-primary/40 pl-3 text-muted italic">
                      {line.slice(2)}
                    </blockquote>
                  );
                }
                if (line.startsWith('- ') || line.startsWith('✅ ') || line.startsWith('❌ ')) {
                  return <p key={i} className="pl-1">{line}</p>;
                }
                return <p key={i} className={line === '' ? 'my-1' : ''}>{line}</p>;
              })}
            </div>
          </div>
          <button
            onClick={() => setSelectedArticle(null)}
            className="mt-6 w-full rounded-xl border border-border py-3 text-sm text-muted touch-feedback"
          >
            ← 返回列表
          </button>
        </div>
      </div>
    );
  }

  return (
    <div>
      <PageHeader
        title="运营宝库"
        subtitle={`${ARTICLES.length} 篇精选干货 · ${bookmarks.length} 篇已收藏`}
      />

      <div className="px-4 py-4 space-y-4 pb-8">
        {/* Category filter */}
        <div className="flex gap-2 overflow-x-auto pb-1 scrollbar-hide">
          {CATEGORIES.map((c) => (
            <button
              key={c}
              onClick={() => setActiveCategory(c)}
              className={`shrink-0 rounded-full px-3 py-1.5 text-xs font-medium transition-all touch-feedback ${
                activeCategory === c
                  ? "bg-emerald-500 text-white"
                  : "border border-border text-muted"
              }`}
            >
              {c}
            </button>
          ))}
        </div>

        {/* Bookmarks reminder */}
        {bookmarks.length > 0 && activeCategory === "全部" && (
          <div className="rounded-xl bg-emerald-500/10 border border-emerald-500/20 px-4 py-3 flex items-center gap-2">
            <span className="text-lg">🔖</span>
            <p className="text-xs text-emerald-400">
              你已收藏 {bookmarks.length} 篇文章，带 🔖 的是收藏内容
            </p>
          </div>
        )}

        {/* Articles */}
        <div className="space-y-3">
          {filtered.map((article) => (
            <div
              key={article.id}
              onClick={() => setSelectedArticle(article)}
              className="rounded-xl bg-card p-4 cursor-pointer hover:bg-card-hover transition-colors touch-feedback"
            >
              <div className="flex items-start justify-between gap-3">
                <div className="flex-1 min-w-0">
                  <div className="flex items-center gap-2 mb-1.5">
                    <span className={`rounded-full px-2 py-0.5 text-[10px] font-medium ${CATEGORY_COLORS[article.category] ?? "text-muted bg-muted/10"}`}>
                      {article.category}
                    </span>
                    <span className="text-[10px] text-muted">{article.readTime}</span>
                  </div>
                  <p className="text-sm font-semibold leading-snug">{article.title}</p>
                  <p className="mt-1 text-xs text-muted leading-relaxed">{article.summary}</p>
                  <div className="mt-2 flex flex-wrap gap-1">
                    {article.tags.map((t) => (
                      <span key={t} className="text-[10px] text-muted/60">#{t}</span>
                    ))}
                  </div>
                </div>
                <button
                  onClick={(e) => { e.stopPropagation(); toggleBookmark(article.id); }}
                  className="shrink-0 text-lg touch-feedback"
                >
                  {bookmarks.includes(article.id) ? "🔖" : "🤍"}
                </button>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  );
}
